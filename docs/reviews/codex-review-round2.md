# Review Round 2

1. High: restart recovery is still incomplete because discussion state transitions are only persisted at session creation, not when the workflow actually advances. `rehydrateOrchestrationState()` only restarts the watchdog when the persisted `discussion_session.round` is `> 0`, but both `cmdPlan()` and `cmdCreateDiscussion()` still save round `0` and later mutate `round/currentAgent` only in memory. After a restart, active planning/discussion sessions will usually rehydrate as not-started and the watchdog will not resume. References: [planning.ts](/home/pseudo/nanoclaw/src/channels/discord-commands/planning.ts#L121), [planning.ts](/home/pseudo/nanoclaw/src/channels/discord-commands/planning.ts#L151), [planning.ts](/home/pseudo/nanoclaw/src/channels/discord-commands/planning.ts#L209), [discussion.ts](/home/pseudo/nanoclaw/src/channels/discord-commands/discussion.ts#L81), [discussion.ts](/home/pseudo/nanoclaw/src/channels/discord-commands/discussion.ts#L93), [discussion.ts](/home/pseudo/nanoclaw/src/channels/discord-commands/discussion.ts#L409), [stream-watcher.ts](/home/pseudo/nanoclaw/src/channels/discord-commands/stream-watcher.ts#L392).

2. High: the planning and decomposition flow is still inconsistent about the final plan artifact, so `!decompose` can ignore the finalized `plan-v2.md`. Planning now ends with Hermes producing `plan-v2.md`, but completion immediately deletes the in-memory `planningSessions` entries that `cmdDecompose()` uses to discover that file. After that, both auto-detection and fallback plan loading drop back to `control/approved-plan.md` or `control/draft-plan.md`, and nothing copies the finalized `plan-v2.md` there. In practice, `!decompose` after a completed planning session can decompose the stale draft or default streams instead of the final plan. References: [discussion.ts](/home/pseudo/nanoclaw/src/channels/discord-commands/discussion.ts#L107), [planning.ts](/home/pseudo/nanoclaw/src/channels/discord-commands/planning.ts#L249), [workstreams.ts](/home/pseudo/nanoclaw/src/channels/discord-commands/workstreams.ts#L64), [workstreams.ts](/home/pseudo/nanoclaw/src/channels/discord-commands/workstreams.ts#L92).

3. Medium: `discord-review` is still describing a GitHub PR workflow rather than the live Discord workstream review system. It tells Argus to find PRs with `gh pr`, review with `gh pr review`, and merge via `gh pr merge`, but the implemented review path is `task-state.json` driven inside `ws-*` folders with stream-watcher orchestration. That skill is still operationally wrong for the current system. Reference: [SKILL.md](/home/pseudo/nanoclaw/container/skills/discord-review/SKILL.md#L1).

4. Low: the codebase still contains a live user-facing contradiction about project scaffolding. The updated docs/skill say `!create_project` does not create `control/approved-plan.md`, but the control-room welcome embed still says the scaffold includes `draft-plan.md, approved-plan.md`, while `initProjectWorkspace()` only creates directories and coordination files. References: [project.ts](/home/pseudo/nanoclaw/src/channels/discord-commands/project.ts#L123), [helpers.ts](/home/pseudo/nanoclaw/src/channels/discord-commands/helpers.ts#L106).

Tests not run as part of this review.
